version: 0.3
saf_primitives_plug_in:                       # A (lower case) name for the plugin (not really used elsewhere)
    module: saf_primitives_estimator          # The (lower case) Python module name, which actually is the name of the file
    class: SAFPrimitives                      # The class name
#   parameters:                               # optional, input passed to __init__ of the module